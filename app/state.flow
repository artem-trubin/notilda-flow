import app/user;
import app/note;
import app/page;

export {
  State(
    userM : Maybe<User>,
    notesB : DynamicBehaviour<[Note]>,
    currentNoteMB : DynamicBehaviour<Maybe<Note>>,
    currentPageB : DynamicBehaviour<PageType>,
  );

  makeInitialState() -> State;
}

State(
  userM : Maybe<User>,
  notesB : DynamicBehaviour<[Note]>,
  currentNoteMB : DynamicBehaviour<Maybe<Note>>,
  currentPageB : DynamicBehaviour<PageType>,
);

makeInitialState() -> State {
  State(None(), make([]), make(None()), make(PageTypeHome()))
}
