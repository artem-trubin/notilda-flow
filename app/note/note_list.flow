import material/material2tropic;

import app/note/note;
import app/note/note_card;

export {
  makeNoteList(
    notes : [Note],
  ) -> Material;
}

makeNoteList(
  notes : [Note],
) -> Material {

  MLinesA(map(notes, makeNoteCard));
}
